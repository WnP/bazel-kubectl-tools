"""Root BUILD file for kubectl_tools module."""

# This file exists to satisfy Bazel's requirement that every .bzl file
# must have a corresponding BUILD file in the same or parent directory.
# The defs.bzl file provides the public API for this module.

exports_files(["defs.bzl"])
